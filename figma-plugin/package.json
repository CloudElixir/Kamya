{
  "name": "kamya-figma-generator",
  "version": "1.0.0",
  "description": "Generates complete KAMYA astrology app UI in Figma",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=code.js --target=es2017",
    "watch": "esbuild src/main.ts --bundle --outfile=code.js --target=es2017 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
